# build dependencies
scikit-build-core[pyproject]
cython
cython-cmake
jsonschema  # needed by mbedtls's own build-time codegen (driver wrappers)

# development dependencies
build
twine

# testing dependencies
pytest
pytest-cov
